import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourname.interestcal',
  appName: 'Simple Interest Pro',
  webDir: 'dist'
};

export default config;
